node_modules/
dist/
logs/
*.log
.env
.env.*
!.env.example
.DS_Store

# Local toolchain (Node binary, npm cache, pm2 state) — portable, never committed.
.tooling/

# Local admin database (credentials, spaces, disks) — never commit.
server/admin.db
server/admin.db-shm
server/admin.db-wal
server/backups/
